swiftmailer:
    url: '%env(MAILER_URL)%'
    spool: { type: memory }
    transport: smtp
    host: smtp.gmail.com
    port: 587
    encryption: tls
    auth_mode: login
    username: your_gmail_username
    password: your_gmail_password